{% extends '::base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    {{ form_row(form.username, {'value' : user.username}) }}
    {{ form_row(form.wallet, {'value' : user.wallet}) }}
    {{ form_rest(form) }}
    <input type="submit" value="Edit">
    {{ form_end(form) }}
{% endblock %}

